# waybar yesplaymusic lyrics script

Usage:
cp lyrics to ~/.config/waybar/scripts/, add follow lines into your config.jsonc
```
    "custom/yesplaymusic" : {
        "exec": "~/.config/waybar/scripts/lyrics",
        "restart-interval" : 1,
    }
```
